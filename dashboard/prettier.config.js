//  @ts-check

/** @type {import('prettier').Config} */
const config = {
    semi: false,
    singleQuote: true,
    trailingComma: "all",
    useTabs: false,
    tabWidth: 4
};

export default config;
